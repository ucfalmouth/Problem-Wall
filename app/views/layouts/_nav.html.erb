<nav class="top-bar" data-topbar>
  <ul class="title-area">
    <li class="name">
      <h1><a href="#"><%= link_to "Problem Wall", root_path %></a></h1>
    </li>
     <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Right Nav Section -->
    <ul class="right">
      <% if user_signed_in? %>
      <li class="active"><a href="">Votes Cast: <%= current_user.votes.size %></a></li>

      <li class="has-dropdown">
        <a href="#"><%= current_user.name %></a>
        <% end %>
        <ul class="dropdown">
          <li><a href="#"><%= link_to 'Edit profile', edit_user_registration_path%></a></li>
          <li><a href="#"><%= link_to "Sign out", destroy_user_session_path, method: :delete %></a></li>
        </ul>
      </li>
    </ul>
  </section>
</nav>
